import 'package:flutter/material.dart';

class AppColors {
  // Brand colors
  static const Color primary = Color(0xFF1A998E); // teal
  static const Color secondary = Color(0xFF82CEC6); // lighter teal

  // Neutral colors
  static const Color black = Color(0xFF000000);
  static const Color white = Color(0xFFFFFFFF);

  // Backgrounds
  static const Color lightBackground = white; // pure white
  static const Color darkBackground = black; // pure black for high contrast

  // Text colors
  static const Color lightText = black;
  static const Color darkText = white;

  // Surfaces (cards, sheets, etc.)
  static const Color lightSurface = Color(0xFFF5F5F5); // very light grey
  static const Color darkSurface = Color(0xFF1E1E1E); // soft dark grey
}
